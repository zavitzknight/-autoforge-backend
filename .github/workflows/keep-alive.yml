name: Keep Autoforge Warm

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send heartbeat
        run: |
          curl -fsS https://autoforge-backend.onrender.com > /dev/null
